package max_sk.MyWork.repositories;

import max_sk.MyWork.model.TovarModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TovarRepository extends JpaRepository<TovarModel, Long> {
}
